import { ContractAddressQuery } from "./types";
import { Coin } from "@cosmjs/proto-signing";
export type StrategyType = "Anchor";
export interface VaultStrategy {
    address: string;
    strategyType: StrategyType;
}
export interface QueryVaultBalance extends ContractAddressQuery {
    address: string;
}
export interface QueryVaultBalanceResponse {
    balance: Coin[];
}
export declare const QUERY_VAULT_BALANCE: string;
export declare function queryBalance(contractAddress: string, address: string): Promise<Coin[]>;
export interface QueryVaultStrategyAddress extends ContractAddressQuery {
    strategy: StrategyType;
}
export interface QueryVaultStrategyAddressResponse {
    strategyAddress: VaultStrategy;
}
export declare const QUERY_VAULT_STRATEGY_ADDRESS: string;
export declare function queryStrategyAddress(contractAddress: string, strategy: StrategyType): Promise<VaultStrategy>;
